services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fiyatradar
      POSTGRES_PASSWORD: fiyatradar
      POSTGRES_DB: fiyatradar
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
