// Point this at the machine running the backend (not "localhost" when testing on a
// physical device via Expo Go - use your computer's LAN IP instead, e.g. 192.168.1.23).
export const API_BASE_URL = "http://localhost:8000/api";
